import { SiteShell } from '@/shared/components/layout/SiteShell';
import { PlaceholderPage } from '@/shared/components/PlaceholderPage';

const ContractSignaturePage = () => (
  <SiteShell>
    <PlaceholderPage layer="portal" title="Assinatura de contrato" description="Fluxo de assinatura digital — disponível após emissão do contrato pelo administrativo." />
  </SiteShell>
);

export default ContractSignaturePage;