-- noop migration placeholder (não há mudança de schema). Mantido para referência: 
-- a aceitação de divergência de endereço é feita via edge `qa-processo-doc-aceitar-divergencia` 
-- usando service_role após validar dono do processo.
SELECT 1;